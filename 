#include "Zombie.h"
#include "Zombie.hpp"


Zombie	*zombieHorde(int N, std::string name)
{
	Zombie	*horde = new (std::nothrow) Zombie[N]{Zombie(name)};
	//if (horde != NULL)
	//{
	//	for (int i = 0; i < N; i++)
	//		horde[i] = Zombie(name);
	//	return (horde);
	//}
	return (horde);
}
